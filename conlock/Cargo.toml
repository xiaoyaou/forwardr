[package]
name = "conlock"
version = "0.1.0"
edition = "2024"

[features]
default = ["wait"]
sync = []
wait = ["dep:atomic-wait", "sync"]
async = []

[dependencies]
atomic-wait = { version = "1.1.0", optional = true }
